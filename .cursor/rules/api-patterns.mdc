---
description: API 调用和错误处理模式
globs: "**/*.js"
alwaysApply: false
---

# API 调用模式

## 端点定义

所有端点在 `src/utils/pathMap.js` 定义:

```javascript
import {BASE_URI, BASE_HOST, GET, POST} from '../../utils/pathMap';

const response = await axios.get(BASE_URI + GET.CLUB_INFO_ALL);
// 注意: BASE_URI 包含尾部斜杠
```

## 错误处理

**用户可见错误必须显示 Alert 或 Toast**:

```javascript
try {
    // API 调用
} catch (error) {
    Alert.alert(
        'Error Title',
        'Descriptive error message.\nPlease check network...',
        null,
        {cancelable: true},
    );
    // 或
    Toast.show({type: 'error', text1: 'Error', text2: 'Message'});
}
```
