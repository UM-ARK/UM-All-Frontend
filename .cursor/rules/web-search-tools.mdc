---
description: 聯網搜尋工具使用規範
alwaysApply: true
---

# 聯網搜尋工具使用規範

本規則指導何時使用 web_search、understand_image、Perplexity 等工具取得最新資訊和解決問題。

## 一、觸發條件

遇到以下情況時，**必須**使用聯網搜尋工具：

### 1. 前端開發相關任務

- 查找第三方庫的**最新文件**、API 用法、範例程式碼
- 搜尋**社群討論**、Stack Overflow、GitHub Issues
- 查找 **bug 解決方案**、已知問題、workaround
- 搜尋**最佳實踐**、設計模式、效能最佳化方案

### 2. 使用者明确要求

當使用者要求以下關鍵字時：
- 「最新」、「在線」、「線上」
- 「查一下」、「搜尋」、「找一下」
- 「有沒有最新的」、「現在的」
- 任何要求取得即時/最新資訊的需求

### 3. 不確定的技術問題

- 不確定某個 API 的目前用法
- 遇到陌生的錯誤訊息
- 需要確認某個庫/框架的版本相容性

## 二、工具選擇優先級

### 第一步：Omnisearch（優先使用）

**優先使用** `user-omnisearch-ai_search` 或 `user-omnisearch-web_search` 進行搜尋，這些工具可以搜尋**國外資料**：

```javascript
// AI 語意搜尋（推薦用於技術問題）
user-omnisearch-ai_search({
  query: 'React Native FlashList vs FlatList performance benchmark 2026',
  provider: 'perplexity'
})

// 網頁搜尋
user-omnisearch-web_search({
  query: 'React Native 0.81 new features 2026',
  provider: 'brave' // 或 'tavily'
})
```

**適用場景**：
- 查找**國外技術文件**、Stack Overflow、GitHub
- 取得**最新的國際技術動態**
- 需要更全面、更深入的技術分析

**Provider 選擇建議**：
| Provider | 特點 | 適用場景 |
|----------|------|----------|
| `perplexity` | 即時 + 推理能力 | 技術問題、深度分析 |
| `brave` | 隱私保護、支援查詢運算子 | 精確搜尋 |
| `tavily` | 事實性搜尋 | 查找準確的技術資訊 |

### 第二步：Minimax Web Search（備用）

**僅在以下情況**使用 `user-MiniMax-web_search`：
- 需要**國內技術文件**（如 CSDN、百度百科、掘金）
- 查找**國內特有的技術問題**討論
- 搜尋**中文技術社群**的解決方案

```javascript
// 國內技術文件搜尋
user-MiniMax-web_search({
  query: 'React Native 中文網 教程'
})
```

### 第三步：圖片理解（understand_image）

當需要理解、分析圖片內容時：

```javascript
// 分析 UI 設計圖
user-MiniMax-understand_image({
  image_source: 'https://example.com/design.png',
  prompt: '分析這個 React Native 元件的佈局和樣式特點'
})

// 分析錯誤截圖
user-MiniMax-underverse_image({
  image_source: '/path/to/error.png',
  prompt: '這個錯誤訊息是什麼，如何解決'
})
```

**適用場景**：
- 使用者提供 UI 設計圖，需要理解佈局
- 錯誤截圖、崩潰日誌圖片
- 需要從圖片中擷取技術資訊

## 三、搜尋策略

### 搜尋優先級

1. **優先使用 Omnisearch** - 搜尋國外資料，取得更全面、更權威的技術資訊
2. **僅在需要國內文件時使用 Minimax** - 如 CSDN、掘金、百度百科等中文技術資源

### 高效搜尋技巧

| 場景 | 搜尋關鍵字 |
|------|-----------|
| 最新文件 | `[庫名] documentation 2026` |
| API 用法 | `[庫名] `[API名稱]` example` |
| 問題解決 | `[庫名] `[錯誤訊息]` github issue` |
| 版本相容性 | `[庫名] `[版本1]` vs `[版本2]` compatibility` |
| 效能最佳化 | `[框架] performance best practices 2026` |

### 搜尋結果處理

1. **優先選擇官方文件**：官方文件最權威、最及時
2. **查看 GitHub Issues**：確認是否為已知 bug
3. **檢查更新時間**：確保資訊是最新的
4. **記錄來源**：將關鍵資訊記錄到 Serena MCP Memory

## 四、實際應用範例

### 範例 1：使用者要求查找最新文件

使用者：「幫我查一下 react-native-reanimated v4 的最新用法」

```javascript
// ✅ 正確：優先使用 Omnisearch 搜尋國外資料
user-omnisearch-ai_search({
  query: 'react-native-reanimated v4 latest documentation 2026',
  provider: 'perplexity'
})
```

### 範例 2：遇到不確定的 API

使用者：「這個動畫效果怎麼實現？」

```javascript
// ✅ 正確：使用 Omnisearch 搜尋最佳實現方式
user-omnisearch-web_search({
  query: 'React Native smooth animation transition best practices 2026',
  provider: 'brave'
})
```

### 範例 3：需要國內技術文件

使用者：「幫我查一下 React Native 中文網的教程」

```javascript
// ✅ 正確：使用 Minimax 搜尋國內資料
user-MiniMax-web_search({
  query: 'React Native 中文網 教程'
})
```

### 範例 4：Omnisearch 結果不足

```javascript
// 第一步：Omnisearch AI 搜尋
user-omnisearch-ai_search({
  query: 'expo router v4 static vs dynamic routes comparison',
  provider: 'perplexity'
})
// 結果不夠詳細

// 第二步：嘗試其他 provider
user-omnisearch-web_search({
  query: 'expo router v4 static vs dynamic routes',
  provider: 'tavily'
})
```

### 範例 5：使用者發送設計圖

使用者：[發送 UI 設計圖]

```javascript
// ✅ 正確：理解圖片內容
user-MiniMax-underverse_image({
  image_source: '使用者發送的圖片',
  prompt: '分析這個行動端介面的元件結構，使用什麼技術實現'
})
```

## 五、禁止行為

- ❌ **不要**僅憑記憶回答可能過時的技術問題
- ❌ **不要**假設某個 API 的用法沒有變化
- ❌ **不要**在使用者要求「最新」時仍使用訓練資料回答
- ❌ **不要**忽略使用者發送的圖片而不理解其內容

## 六、資訊記錄

搜尋到重要資訊後，應記錄到 Serena MCP Memory：

```markdown
# [技術名稱] [主題]

## 來源
- **文件 URL**: [搜尋到的連結]
- **搜尋時間**: 2026-02-13

## 關鍵內容
[記錄重要發現]

## 本專案應用
[如何應用到目前專案]
```

---

**記住**：前端技術迭代快，最好的答案是**最新的、來自權威來源的**答案。聯網搜尋不是「不會」的表現，而是「專業」的表現。
