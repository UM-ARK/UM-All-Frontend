---
description: 本地存储规范 - 禁止直接使用 AsyncStorage
globs: "**/*.js"
alwaysApply: false
---

# 存储规范

**禁止直接使用 AsyncStorage**，必须使用 `storageKits.js`:

```javascript
import {
    getLocalStorage,
    setLocalStorage,
    handleLogin,
    handleLogout,
    updateUserInfo,
} from '../../utils/storageKits';

// 数据持久化
await setLocalStorage('key', object);
const data = await getLocalStorage('key');

// 认证（会通过 reloadAppAsync 重启应用）
await handleLogin(userInfo);
await handleLogout();

// 实时更新（无需重启）
await updateUserInfo(userInfo);
```

## 课程数据管理

使用 `checkCoursesKits.js` 管理课程数据:

```javascript
import {
    getCourseData,
    needUpdate,
    saveCourseDataToStorage,
    checkCloudCourseVersion,
} from '../../utils/checkCoursesKits';

const courses = getCourseData('pre'); // 或 'adddrop'
```

**禁止直接写入**: `offer_courses`, `course_plan`, `course_plan_time` 键
