---
description: 国际化 (i18n) 使用规范
globs: "**/*.js"
alwaysApply: false
---

# 国际化规范

## 翻译文件

- **英文**: `src/i18n/en-us.json`
- **繁体中文**: `src/i18n/zh-hk.js`

## 使用方式

```javascript
import {useTranslation} from 'react-i18next';

// 单个命名空间
const {t} = useTranslation('common');

// 多个命名空间
const {t} = useTranslation(['common', 'home']);
```

## 键名规范

**使用繁体中文作为键名**:

```javascript
// ✅ 正確
t('資訊')
t('設置')

// ❌ 錯誤
t('info')
t('settings')
```

## 命名空间分组

| 命名空间 | 用途 |
|---------|------|
| `common` | 通用（按钮、提示） |
| `home` | 主页 |
| `setting` | 设置 |
| `catalog` | 课表 |
| `club` | 组织 |
| `features` | 功能服务 |
