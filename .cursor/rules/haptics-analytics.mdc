---
description: 觸覺回饋和 Firebase Analytics 規範
globs: "**/*.js"
alwaysApply: false
---

# 觸覺回饋和分析

## 觸覺回饋

**所有使用者互動必須呼叫 `trigger()`**:

```javascript
import { trigger } from '../../utils/trigger';
import { Pressable } from 'react-native';

<Pressable onPress={() => { 
    trigger(); 
    handleAction(); 
}}>
```

適用於：按鈕按下、標籤切換、列表項點擊等

## Firebase Analytics

使用 `logToFirebase()` 記錄事件:

```javascript
import {logToFirebase} from '../../utils/firebaseAnalytics';

// 頁面瀏覽
logToFirebase('screen_view', {screen_name: 'ClubDetail'});

// 自訂事件
logToFirebase('button_click', {button_name: 'submit'});
```
