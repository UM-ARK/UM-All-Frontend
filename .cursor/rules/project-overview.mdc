---
description: ARK ALL React Native 專案概覽和建構命令
alwaysApply: true
---

# ARK ALL 專案概覽

- **類型**: React Native 0.86 + Expo SDK 57 行動應用
- **名稱**: ARK ALL (澳門大學學生應用)
- **版本**: 26.5.0
- **平台**: iOS, Android
- **套件管理器**: pnpm

## 建構命令

```bash
pnpm install          # 安裝相依 (Node ≥18)
pnpm ios              # iPhone 16 Pro 模擬器
pnpm iosNew           # iPhone 17 Pro 模擬器
pnpm iosBig           # iPad Pro 模擬器
pnpm iosTrue          # 實體裝置
pnpm android          # Android (API 31/33)
pnpm lint             # ESLint 檢查
pnpm start            # 啟動 Metro
```

## 開發工作流程

- **熱重載**: Metro 自動熱重載 JS 變更，無需重新執行 `pnpm ios`
- **僅需重啟 Metro**: 遇到 Metro 錯誤或需清理快取時
- **僅需重新建構**: 新增/刪除原生相依、修改 `app.json` 或 `package.json`

## Expo CNG 工作流程

1. 無需手動 `pod install` - Expo CNG 自動管理
2. iOS/Android 原生檔案從設定產生
3. 大部分原生設定在 `app.json` 中設定
