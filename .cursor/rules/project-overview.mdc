---
description: ARK ALL React Native 專案概覽和建構命令
alwaysApply: true
---

# ARK ALL 專案概覽

- **類型**: React Native 0.81.5 + Expo SDK 55 行動應用
- **名稱**: ARK ALL (澳門大學學生應用)
- **版本**: 26.1.1
- **平台**: iOS, Android
- **架構**: 離線優先 + Cloudflare Workers API，6小時自動更新

## 建構命令

```bash
yarn install          # 安裝相依 (Node ≥18)
yarn ios              # iPhone 16 Pro 模擬器
yarn iosNew           # iPhone 17 Pro 模擬器
yarn iosBig           # iPad Pro 模擬器
yarn iosTrue          # 實體裝置
yarn android          # Android (API 31/33)
yarn lint             # ESLint 檢查
yarn start            # 啟動 Metro
```

## 開發工作流程

- **熱重載**: Metro 自動熱重載 JS 變更，無需重新執行 `yarn ios`
- **僅需重啟 Metro**: 遇到 Metro 錯誤或需清理快取時
- **僅需重新建構**: 新增/刪除原生相依、修改 `app.json` 或 `package.json`

## Expo CNG 工作流程

1. 無需手動 `pod install` - Expo CNG 自動管理
2. iOS/Android 原生檔案從設定產生
3. 大部分原生設定在 `app.json` 中設定
