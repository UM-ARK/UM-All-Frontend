---
description: 代碼風格和命名規範
globs: "**/*.js"
alwaysApply: false
---

# 代碼風格

## Import 順序

1. React Native
2. 第三方庫
3. 本地模組
4. utils/components

使用相對路徑 `../../`，不用絕對路徑。

## 命名規範

| 類型 | 規範 | 範例 |
|------|------|------|
| 元件/頁面 | PascalCase | `ClubDetail.js` |
| 函數/變數 | camelCase | `handleLogin` |
| 常數 | UPPER_SNAKE_CASE | `BASE_URI` |

## 互動元件規範：使用 Pressable

**新程式碼和重構必須使用 `Pressable`，禁止引入新的 `TouchableOpacity` 系列元件**。

```javascript
import { Pressable } from 'react-native';

// ✅ 正確：使用 Pressable
<Pressable onPress={() => { trigger(); handleAction(); }}>
    <Text>按鈕</Text>
</Pressable>

// ✅ 正確：Pressable 自訂按壓效果
<Pressable
    onPress={handlePress}
    style={({ pressed }) => [
        styles.button,
        pressed && { opacity: 0.7 },
    ]}
>
    <Text>按鈕</Text>
</Pressable>

// ❌ 禁止：在新程式碼中使用 TouchableOpacity
import { TouchableOpacity } from 'react-native';
<TouchableOpacity onPress={handlePress}>...</TouchableOpacity>

// ❌ 禁止：在新程式碼中使用其他 Touchable 系列
import { TouchableHighlight, TouchableWithoutFeedback } from 'react-native';
```

| 元件 | 狀態 |
|------|------|
| `Pressable` | ✅ 新程式碼必須使用 |
| `TouchableOpacity` | ❌ 僅存量程式碼保留，重構時替換 |
| `TouchableHighlight` | ❌ 禁止使用 |
| `TouchableWithoutFeedback` | ❌ 禁止使用 |
| `TouchableNativeFeedback` | ❌ 禁止使用 |
| `TouchableScale`（第三方） | ⚠️ 存量保留，新程式碼用 Pressable + Reanimated 替代 |

> **注意**：存量程式碼中的 `TouchableOpacity` 不需要立即全部替換，在重構相關元件時順帶遷移即可。

## Prettier 設定

```javascript
{
  singleQuote: true,
  trailingComma: 'all',
  tabWidth: 4,
  arrowParens: 'avoid',
  bracketSpacing: false,
  bracketSameLine: true
}
```
