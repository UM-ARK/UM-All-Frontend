---
description: Git commit message 生成规则 - 使用 Gitmoji 规范
alwaysApply: true
---

# Git Commit Message 规则

为了使提交历史更清晰易读，我们使用 Gitmoji (https://gitmoji.dev/) 来标准化提交消息的格式。

## 核心要求

当你帮助用户生成 Git commit message 时，**必须**遵循以下规则。

## 格式规范

```
:gitmoji: 简明描述 (最多 50 字符)

详细描述（可选，换行后）
```

## 必须使用的 Gitmoji 前缀

根据变更类型选择正确的 Gitmoji：

| 变更类型 | Gitmoji | 说明 |
|---------|----------|------|
| 新功能 | ✨ `:sparkles:` | 添加新功能 |
| Bug 修复 | 🐛 `:bug:` | 修复 bug |
| 代码重构 | ♻️ `:recycle:` | 重构代码 |
| UI/样式更新 | 💄 `:lipstick:` | 界面样式改变 |
| 性能优化 | ⚡️ `:zap:` | 性能提升 |
| 文档更新 | 📝 `:memo:` | 添加文档 |
| 代码删除 | 🔥 `:fire:` | 删除代码/文件 |
| 添加依赖 | ➕ `:heavy_plus_sign:` | 新增依赖 |
| 删除依赖 | ➖ `:heavy_minus_sign:` | 移除依赖 |
| 升级依赖 | ⬆️ `:arrow_up:` | 升级依赖 |
| 降级依赖 | ⬇️ `:arrow_down:` | 降级依赖 |
| 配置文件 | 🔧 `:wrench:` | 配置文件的修改 |
| 代码格式 | 🎨 `:art:` | 代码格式优化 |
| 添加测试 | ✅ `:white_check_mark:` | 添加/更新测试 |
| 失败测试 | 🧪 `:test_tube:` | 添加失败的测试 |
| 紧急修复 | 🚑️ `:ambulance:` | 紧急修复 |
| 简单修复 | 🩹 `:adhesive_bandage:` | 简单修复（非关键问题） |
| 部署相关 | 🚀 `:rocket:` | 部署相关 |
| CI 构建 | 👷 `:construction_worker:` | 添加/更新 CI 构建系统 |
| 编译文件 | 📦️ `:package:` | 添加/更新编译文件或包 |
| 添加注释 | 💡 `:bulb:` | 添加/更新代码注释 |
| 进行中 | 🚧 `:construction:` | 进行中的工作 |
| 弃用代码 | 🗑️ `:wastebasket:` | 弃用需要清理的代码 |
| 合并分支 | 🔀 `:twisted_rightwards_arrows:` | 合并分支 |
| 回滚变更 | ⏪️ `:rewind:` | 回滚变更 |

## 输出格式示例

```
✨ 添加课程搜索功能

- 实现课程搜索界面
- 添加搜索结果展示
- 优化搜索算法
```

```
🐛 修复课程列表加载失败

- 修复网络请求超时处理
- 优化错误提示信息
```

```
♻️ 重构课程数据处理逻辑

- 提取公共方法
- 优化性能
```

## 禁止事项

- ❌ 不要使用 `feat:`、`fix:` 等传统前缀
- ❌ 不要省略 Gitmoji
- ❌ 不要使用未在列表中的 emoji

## 最佳实践

- 每个提交应专注于单一功能或修复
- 使用英文或繁体中文写提交消息
- 简明描述应清晰说明变更的内容
- 详细描述可包含变更的原因和影响

## 合并 PR 时保留原作者信息

### 重要要求

当合并 PR 提交时，**必须**保留该 PR 的原作者信息，不要用自己的信息覆盖原作者。

### 如何在 GitHub 中保留原作者信息

#### 方法 1：使用 GitHub 网页界面合并（推荐）
使用 GitHub 网页界面的 "Merge pull request" 按钮，系统会自动保留原作者信息。

#### 方法 2：使用命令行合并时保留原作者
如果使用命令行合并，使用 `--no-ff` 和正确的作者信息：

```bash
# 切换到目标分支
git checkout main

# 合并 PR 分支，保留原作者信息
git merge --no-ff pr-branch-name

# 确保提交作者是原作者
git commit --amend --author="Original Author <author@example.com>"
```

### GitHub CLI 合并
使用 GitHub CLI 合并时，系统也会自动保留原作者信息：

```bash
gh pr merge <pr-number> --merge
```

### 生效条件
此规则适用于：
- 合并任何有作者的 PR 提交
- 使用任何方式合并 PR 时

## 生效条件

此规则适用于：
- 用户要求生成 commit message
- 用户点击 Cursor 的 "Generate Commit Message" 按钮
- 用户要求你帮助 commit 代码
- 合并有作者的 PR 提交
