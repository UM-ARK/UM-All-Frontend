---
description: iOS 26 原生特性、液态玻璃设计规范与 2026 现代化 UI 指引
globs: "**/*.js"
alwaysApply: true
---

# iOS 26 原生特性与现代化 UI 设计规范

## 核心目标

**APP 正在进行现代化 UI 重构**，目标是引入 2026 年流行的设计趋势，特别是 iOS 26 原生新特性。所有 UI 改动都应遵循以下优先级：

1. 拥抱平台原生特性（iOS 26 液态玻璃、原生导航按钮等）
2. 保持跨平台优雅降级（Android / 旧 iOS 有合理 fallback）
3. 遵循项目现有主题色体系（ThemeContext），必要时提议新配色

---

## 一、`isLiquidGlassSupported` 平台检测

### 唯一检测方式

```javascript
import { isLiquidGlassSupported } from '@callstack/liquid-glass';
```

| 平台 | 结果 |
|------|------|
| iOS 26+ | `true` |
| iOS < 26 | `false` |
| Android 所有版本 | `false` |

### 禁止的检测方式

```javascript
// ❌ 绝对禁止：使用 Platform 判断版本
const isIOS26 = Platform.OS === 'ios' && parseInt(Platform.Version, 10) >= 26;

// ❌ 禁止：硬编码平台判断
if (Platform.OS === 'ios') { /* 液态玻璃逻辑 */ }
```

---

## 二、`LiquidGlassView` 组件规范

### 基本用法

```javascript
import { isLiquidGlassSupported, LiquidGlassView } from '@callstack/liquid-glass';
import { Pressable } from 'react-native';

// 圆形按钮示例（如浮动操作按钮、关闭按钮）
<LiquidGlassView
    interactive={true}
    hover={isLiquidGlassSupported ? { effect: 'highlight' } : null}
    style={{
        backgroundColor: isLiquidGlassSupported ? null : theme.white,
        borderRadius: scale(25),
        width: scale(50),
        height: scale(50),
        justifyContent: 'center',
        alignItems: 'center',
        overflow: 'hidden',
    }}
>
    <Pressable onPress={handlePress}>
        <Ionicons name="icon-name" color={theme.themeColor} size={scale(24)} />
    </Pressable>
</LiquidGlassView>
```

### 关键模式

| 属性 | iOS 26+ | 旧平台 fallback |
|------|---------|-----------------|
| `backgroundColor` | `null`（液态玻璃自带材质） | `theme.white` / `theme.glass` / `theme.bg_color` |
| `hover` | `{ effect: 'highlight' }` | `null` |
| `interactive` | `true` | `true`（组件自动降级为普通 View） |

### 重要提醒

- `LiquidGlassView` 在不支持的平台上会自动降级为普通 `View`，因此可以安全使用
- 但 `backgroundColor` 等视觉属性**必须手动处理 fallback**
- `hover` 属性仅在 `isLiquidGlassSupported` 时设置

---

## 三、适用场景指引

### ✅ 推荐使用液态玻璃的场景

| 场景 | 原因 | 项目示例 |
|------|------|---------|
| **导航栏 (Header)** | 原生一致性，iOS 26 系统级支持 | `Nav.js` - `headerTransparent` |
| **底部标签栏 (Tab Bar)** | 原生一致性 | `Tabbar.js` - `translucent` |
| **浮动操作按钮 (FAB)** | 悬浮元素适合玻璃态 | `ScrollToTopButton.js` |
| **图片查看器叠加层按钮** | 覆盖在内容上的控件 | `ARKImageView.js` |
| **全屏弹窗内的操作按钮** | 强调操作入口的同时保持通透 | — |
| **工具栏** | 浮动工具栏、底部操作栏 | — |

### ❌ 不推荐使用液态玻璃的场景

| 场景 | 原因 |
|------|------|
| **列表项 / 卡片内部** | 大量重复使用会导致视觉混乱和性能问题 |
| **文字标签 / Badge** | 元素太小，液态玻璃效果不明显 |
| **整个页面背景** | 不符合液态玻璃「点缀」的设计理念 |
| **输入框** | 影响可读性，不适合文字输入场景 |
| **列表分割线 / 小型装饰** | 性能浪费，视觉收益低 |
| **已有明确背景色的区域** | 液态玻璃与实色背景冲突 |

### 设计原则

> **液态玻璃是"高光点缀"，不是"全面覆盖"**
> 
> 每个页面使用液态玻璃的元素应控制在 **2-4 个**关键交互点。过多使用会让界面显得廉价，而非现代。

---

## 四、降级策略

### 导航系统降级

```javascript
// Nav.js / Tabbar.js
headerTransparent: isLiquidGlassSupported,
headerBlurEffect: isLiquidGlassSupported ? null : 'systemThinMaterial',

// Tab Bar
translucent: isLiquidGlassSupported,
```

### 组件降级

```javascript
// 浮动按钮 - 降级为实色 + 阴影
<LiquidGlassView style={{
    backgroundColor: isLiquidGlassSupported ? null : theme.white,
    // 非液态玻璃时添加阴影提升层次感
    ...(isLiquidGlassSupported ? {} : {
        shadowColor: theme.black.main,
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.1,
        shadowRadius: 4,
        elevation: 3,
    }),
}}>
```

### 内容区域降级

```javascript
// ScrollView contentInsetAdjustmentBehavior
contentInsetAdjustmentBehavior={isLiquidGlassSupported ? null : "automatic"}
```

### 降级视觉标准

| 组件类型 | iOS 26+ | iOS < 26 | Android |
|---------|---------|----------|---------|
| 导航栏 | 液态玻璃透明 | BlurView 模糊 | 实色 `bg_color` |
| 浮动按钮 | 液态玻璃 | `theme.glass` 半透明 | `theme.white` + 阴影 |
| 工具栏 | 液态玻璃 | `theme.glass` | 实色 + elevation |

---

## 五、React Navigation 原生适配

### 原生按钮

在 iOS 26+ 上，React Navigation V7 自动支持原生风格按钮。应优先使用 React Navigation 提供的原生组件：

```javascript
import { Button, HeaderBackButton } from '@react-navigation/elements';

// 使用原生返回按钮
headerLeft: () => <HeaderBackButton onPress={navigation.goBack} />,

// 使用原生按钮（自动适配 iOS 26 液态玻璃样式）
headerRight: () => <Button onPress={handleAction}>Action</Button>,
```

### 导航级液态玻璃配置

```javascript
// 在 screenOptions 中统一配置
screenOptions={{
    headerTransparent: isLiquidGlassSupported,
    headerTitleAlign: 'center',
    headerBackButtonDisplayMode: 'minimal',
}}
```

---

## 六、2026 现代化 UI 设计原则

### 设计趋势

1. **材质层次感** — 用液态玻璃、模糊、半透明区分 UI 层级
2. **大圆角与柔和阴影** — 卡片圆角 `scale(12)` - `scale(16)`，阴影柔和
3. **留白与呼吸感** — 元素间距充足，避免拥挤
4. **动效自然** — 使用 `react-native-reanimated` 实现流畅过渡
5. **平台原生感** — iOS 上尽量使用原生组件和特性，Android 遵循 Material Design 3

### 改动尺度分级

| 级别 | 描述 | 是否需讨论 |
|------|------|-----------|
| 小幅优化 | 调整间距、圆角、字重 | 直接实施 |
| 组件重构 | 重写列表项、卡片、按钮样式 | 简要说明意图 |
| 配色调整 | 引入新颜色或修改现有配色 | **必须先发起讨论** |
| 布局重构 | 整页布局改动 | **必须先发起讨论** |

---

## 七、配色方案演进流程

### 现有主题色必须尊重

当前 ThemeContext 定义的颜色体系是基础，**任何配色改动都必须在 ThemeContext 中操作**。

### 提议新配色的流程

1. **说明需求**：为什么现有颜色不满足（哪个场景、什么视觉效果）
2. **提供方案**：新颜色的亮色/暗色值，以及适用场景
3. **对比展示**：与现有配色的对比，确保协调
4. **用户确认后**：在 `ThemeContext.js` 的 `getColorDiy` 中添加

```javascript
// 提议示例：
// 需求: 卡片需要更轻的背景色以突出内容层次
// 方案: cardBg - 亮色 '#F8F9FA' / 暗色 '#1E1E1E'
// 影响范围: 信息卡片、课程卡片

const getColorDiy = (isLight) => ({
    // ... 现有颜色 ...
    cardBg: isLight ? '#F8F9FA' : '#1E1E1E',
});
```

### 配色讨论触发条件

当 AI 助手遇到以下情况时，**应主动发起配色讨论**：

- 现有颜色无法实现设计目标
- 发现新页面需要 ThemeContext 中未定义的语义颜色
- 用户要求的 UI 效果需要新增颜色层级
- 暗色模式下现有颜色对比度不足

---

## 八、原生特性采纳通用原则

### 采纳标准

| 条件 | 说明 |
|------|------|
| 有官方库支持 | 如 `@callstack/liquid-glass` |
| 降级方案明确 | 不支持的平台有合理 fallback |
| 不破坏跨平台一致性 | 核心功能在所有平台可用 |
| 性能影响可控 | 不引入明显的性能回退 |

### 禁止行为

- ❌ 为单一平台引入仅该平台可用的核心功能
- ❌ 使用未经社区验证的原生特性桥接
- ❌ 在降级方案不明确时引入新原生特性
