---
description: 使用 react-native-debugger-mcp 工具读取 React Native 台日志
always模拟器控制Apply: false
---

# React Native 模拟器日志读取规则

当需要读取 React Native 模拟器的控制台日志时，使用 `react-native-debugger-mcp` 工具。

## 工具函数

| 函数 | 说明 |
|------|------|
| `getConnectedApps` | 获取已连接的 React Native 应用列表 |
| `readConsoleLogsFromApp` | 读取指定应用的控制台日志 |

## 使用前提

- Metro 开发服务器正在运行 (`yarn start`)
- React Native 应用已启动并连接到 Metro

## 使用方法

### 1. 获取已连接的应用

```javascript
user-react-native-debugger-mcp-getConnectedApps({
  metroServerPort: 8081  // Metro 端口号，默认 8081
})
```

返回结果包含：
- `id`: 应用连接 ID
- `title`: 应用标题和设备信息
- `description`: React Native 模式描述
- `webSocketDebuggerUrl`: WebSocket 调试地址

### 2. 读取控制台日志

```javascript
user-react-native-debugger-mcp-readConsoleLogsFromApp({
  app: {
    id: "xxx-1",
    description: "React Native Bridgeless [C++ connection]",
    webSocketDebuggerUrl: "ws://localhost:8081/inspector/debug?device=xxx&page=1"
  },
  maxLogs: 50  // 最多读取的日志数量，默认 100
})
```

返回结果为日志数组，每条日志包含：
- `type`: 日志类型 (log, error, warn, info)
- `text`: 日志内容
- `level`: 日志级别
- `timestamp`: 时间戳

## 典型使用场景

1. **调试运行时错误**：读取 error 级别日志
2. **追踪应用行为**：查看 console.log 输出
3. **监控网络请求**：查看 API 调用日志
4. **分析崩溃原因**：查看异常堆栈信息

## 注意事项

- 日志读取需要应用保持运行状态
- `maxLogs` 参数可控制返回的日志数量
- 应用重启后需要重新获取连接信息
