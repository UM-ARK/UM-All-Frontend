---
description: 字型和 UI 尺寸縮放規範
globs: "**/*.js"
alwaysApply: false
---

# 字型和縮放規範

## 文字樣式

所有文字元件必須使用 `uiStyle.defaultText`:

```javascript
import { uiStyle } from '../../components/ThemeContext';
import { scale } from 'react-native-size-matters';

style={{ 
    ...uiStyle.defaultText, 
    fontSize: scale(14), 
    color: theme.black.main 
}}
```

## UI 尺寸縮放

**必須使用 `react-native-size-matters`**:

```javascript
import { scale, verticalScale, moderateScale } from 'react-native-size-matters';

width: scale(100),         // 水平縮放
height: verticalScale(50), // 垂直縮放
fontSize: moderateScale(16) // 字型縮放
```
