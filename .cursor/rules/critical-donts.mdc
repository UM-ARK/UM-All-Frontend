---
description: 关键禁止事项 - 必须遵守
alwaysApply: true
---

# 关键禁止事项

## 绝对禁止

- ❌ **直接使用 AsyncStorage** - 使用 `storageKits.js`
- ❌ **直接写入课程数据键** (`offer_courses`, `course_plan`, `course_plan_time`) - 使用 `checkCoursesKits.js`
- ❌ **删除打包的 JSON 文件** - 它们是离线后备
- ❌ **使用 `COLOR_DIY` from `uiMap.js`** - 使用 `useTheme()`
- ❌ **忘记 `trigger()`** - 所有交互元素都需要触觉反馈
- ❌ **使用 `as any`, `@ts-ignore`** - 正确处理类型
- ❌ **硬编码颜色值** (如 `'#fff'`, `'rgba(...)'`) - 使用 ThemeContext

## 配置要求

- **iOS**: Xcode 10+，Expo CNG 自动管理 CocoaPods
- **Android**: API 31 或 33 模拟器
- **Firebase**: 需要 `google-services.json` 和 `GoogleService-Info.plist`
- **API Token**: 根目录 `umAPIToken.json` 包含 `{"token":"..."}`
