---
description: Jest 在本機／sandbox 因 Watchman 失敗時改用 --watchman=false
alwaysApply: true
---

# Jest 與 Watchman

在 Cursor Agent／sandbox 或本機 Watchman socket 權限異常時，直接跑 `yarn jest` 可能失敗，常見錯誤類似：

```text
fchmod(.../watchman/...-state, 2700): Operation not permitted
watchman --no-pretty get-sockname returned with exit code=1
```

## 正確做法

執行 Jest 時加上 `--watchman=false`：

```bash
# ✅ 推薦（單檔或路徑）
yarn jest src/utils/harbor/__tests__/harborRateLimit.test.js --watchman=false

# ✅ 亦可搭配其他旗標
yarn jest src/utils/harbor/__tests__/harborRateLimit.test.js --no-coverage --watchman=false
```

## 注意

- 先遇到上述 Watchman 錯誤時再加旗標即可；不必為此改 `package.json` 腳本（除非專案另有共識）。
- 這與測試內容無關，是檔案監看後端啟動失敗；關閉 Watchman 後 Jest 會改用節點端掃描，結果仍有效。
